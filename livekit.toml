# LiveKit Agent Deployment Configuration
# This file is used by the LiveKit CLI for agent deployment and management

[project]
# Your LiveKit Cloud project subdomain
# Extract from LIVEKIT_URL in .env.local
# Format: wss://{subdomain}.livekit.cloud
subdomain = "voiceagent-46wqrz65"

[agent]
# Your agent's unique identifier in LiveKit Cloud
# Get this from your LiveKit Cloud dashboard
# You can also run: lk agent config
id = "pattreeya-voice-assistant"
name = "Pattreeya Voice Assistant"